

function scrolltoTop(){
    const element = document.getElementById("hero-section");

    element.scrollIntoView({
        behavior: "smooth"
    });
}



//LANDING PAGE
function scrolltoWorks(){
    const element = document.getElementById("works-section");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoCredential(){
    const element = document.getElementById("credential-section");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoContact(){
    const element = document.getElementById("contact-section");

    element.scrollIntoView({
        behavior: "smooth"
    });
}





//PRODUCT PAGE
function scrolltoRole(){
    const element = document.getElementById("role-section");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoDesigns(){
    const element = document.getElementById("designs-section");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoOther(){
    const element = document.getElementById("other-section");

    element.scrollIntoView({
        behavior: "smooth"
    });
}



function scrolltoStandee(){
    const element = document.getElementById("standee-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoWebsite(){
    const element = document.getElementById("website-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoMobile(){
    const element = document.getElementById("mobile-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoCard(){
    const element = document.getElementById("callingcard-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoLetter(){
    const element = document.getElementById("letterhead-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoBrochure(){
    const element = document.getElementById("brochure-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoLoginSc(){
    const element = document.getElementById("login-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoDashSc(){
    const element = document.getElementById("dashboard-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoProjectSc(){
    const element = document.getElementById("project-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoLibrarySc(){
    const element = document.getElementById("library-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoAdminSc(){
    const element = document.getElementById("admin-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoReportsSc(){
    const element = document.getElementById("reports-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoLandingSc(){
    const element = document.getElementById("landing-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoSignSc(){
    const element = document.getElementById("sign-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoShopSc(){
    const element = document.getElementById("shop-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoCheckSc(){
    const element = document.getElementById("check-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}

function scrolltoFlyerSc(){
    const element = document.getElementById("flyer-container");
    const offset = 118;

    const elementPosition = element.getBoundingClientRect().top + window.pageYOffset;
    const offsetPosition = elementPosition - offset;

    window.scrollTo({
        top: offsetPosition,
        behavior: "smooth"
    });
}